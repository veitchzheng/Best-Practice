server:
  port: 8080
  servlet:
    context-path: /demo

spring:
  application:
    name: spring-boot-demo


# 指定目录输入 debug
logging:
  level:
    com:
      example:
        springbootdemo:
          outer: debug
